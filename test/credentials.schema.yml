root: "pod_credentials"
schemas:
  pod_credentials:
    type: "dict"
    props:
      pod_credential_1:
        type: "str"
        non_empty: true
      pod_credential_2:
        type: "str"
        non_empty: true
      pod_credential_3:
        type: "str"
